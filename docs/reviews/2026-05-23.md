---
hash: "a8cc4b7e"
id: "d9a8e35b"
read_when: "reviewing skill quality history or checking past review findings"
summary: "Review scores across all planning workflow skills with P1/P2/P3 issue counts."
title: "Skill Review — 2026-05-23"
---

# Skill Review — 2026-05-23

## Summary

| Skill | P1 | P2 | P3 |
|-------|----|----|----|
| address-feedback | 0 | 3 | 0 |
| code-review | 0 | 1 | 1 |
| commit-task | 0 | 2 | 1 |
| complete-task | 0 | 1 | 1 |
| delegate-task | 0 | 1 | 0 |
| execute-task | 0 | 2 | 1 |
| executor-status-check | 0 | 2 | 0 |
| new-plan | 0 | 0 | 0 |
| new-solution | 0 | 0 | 0 |
| new-task | 0 | 1 | 0 |
| resolve-comments | 0 | 2 | 0 |
| review-resolve | 1 | 2 | 0 |
| review-task | 0 | 1 | 1 |
| task-feedback-analyser | 0 | 0 | 0 |

## Autoskill Lint Output

```text
[]
```

## Issues by Skill

### address-feedback

#### P2 — Important
- **[Claude]** GraphQL commands use single-quoted query strings while relying on shell variables (`$OWNER`, `$REPO`, `$NUMBER`, `$THREAD_ID`), so variables are not interpolated and fetch/resolve can fail. → Use GraphQL variables with `-F` and declared query parameters (or a safely interpolated heredoc/double-quoted string). (`skills/address-feedback/SKILL.md:30`)
- **[Claude]** Inconsistent PR variable naming (`$NUMBER` vs `$PR`) creates undefined-variable risk. → Standardize on one variable (for example `PR_NUMBER`) across all commands. (`skills/address-feedback/SKILL.md:74`)
- **[OpenAI Codex]** `git add -A` can stage unrelated changes and widen scope. → Stage only files touched for addressed feedback. (`skills/address-feedback/SKILL.md:99`)

### code-review

#### P2 — Important
- **[OpenAI Codex]** Links to an end-to-end pipeline broaden this skill beyond review-only scope. → Keep this skill review-focused; remove or narrow non-review pipeline references. (`skills/code-review/SKILL.md:10`, `skills/code-review/references/workflow-overview.md:8`)

#### P3 — Minor
- **[OpenAI Codex]** Diff guidance hardcodes `main`, reducing portability. → Parameterize base ref (for example `BASE_REF`) and document selection rules. (`skills/code-review/SKILL.md:29`)

### commit-task

#### P2 — Important
- **[Claude]** Failure handling reports checklist errors but lacks a validate-fix-repeat loop. → Add explicit rerun loop until all checks pass before commit. (`skills/commit-task/SKILL.md:29`)
- **[Claude]** Commit message guidance conflicts with referenced conventions, creating ambiguity. → Standardize commit format across main and reference docs or explicitly justify differences. (`skills/commit-task/SKILL.md:35`, `skills/commit-task/references/commit-conventions.md:32`)

#### P3 — Minor
- **[OpenAI Codex]** Referenced commit conventions include execution-phase patterns outside this skill’s scope. → Split or clearly mark out-of-scope sections for planning-doc commits. (`skills/commit-task/references/commit-conventions.md:21`)

### complete-task

#### P2 — Important
- **[Claude]** Workflow can skip persisting `feedback.md` because Step 4 content is not explicitly committed/pushed before merge. → Add explicit post-Step-4 commit/push for `feedback.md` (or broaden Step 3 to include later commits). (`skills/complete-task/SKILL.md:38`, `skills/complete-task/references/template-feedback.md:19`)

#### P3 — Minor
- **[Claude]** Text says “template below” though template is in a reference file. → Point directly to `references/template-feedback.md`. (`skills/complete-task/SKILL.md:42`)

### delegate-task

#### P2 — Important
- **[Claude]** No explicit retry/remediation loop if kickoff verification fails. → Add bounded retries and fallback (next idle pane or structured failure report). (`skills/delegate-task/SKILL.md:56`)

### execute-task

#### P2 — Important
- **[Claude]** Main sequence omits explicit pre-dispatch `git push` required for clean downstream worktree branching. → Add startup step to push to origin and verify branch point assumptions. (`skills/execute-task/SKILL.md:24`, `skills/execute-task/references/worktree-conventions.md:15`)
- **[OpenAI Codex]** PR can be opened without an explicit final repo-level validation gate. → Add final test/lint/typecheck verification loop before PR creation. (`skills/execute-task/SKILL.md:78`)

#### P3 — Minor
- **[Claude]** `AC` acronym is used without expansion. → Expand first use to “acceptance criteria (AC)”. (`skills/execute-task/SKILL.md:23`)

### executor-status-check

#### P2 — Important
- **[Claude]** Status terminology is inconsistent (`running` vs `in progress`). → Choose one canonical term throughout. (`skills/executor-status-check/SKILL.md:3`)
- **[Claude]** Ambiguous-pane handling lacks a concrete iterative feedback loop. → Add explicit numbered decision loop with retry/exit conditions. (`skills/executor-status-check/SKILL.md:38`)

### new-plan

No issues found.

### new-solution

No issues found.

### new-task

#### P2 — Important
- **[OpenAI Codex]** Workflow references `CLAUDE.md` but not `AGENTS.md`, risking missed repository constraints. → Explicitly read `AGENTS.md` (optionally alongside `CLAUDE.md`). (`skills/new-task/SKILL.md:23`)

### resolve-comments

#### P2 — Important
- **[Claude]** Thread-state handling is ambiguous between status updates and append-only history guidance. → Define one canonical model and align all steps/reference text. (`skills/resolve-comments/SKILL.md:32`, `skills/resolve-comments/SKILL.md:47`, `skills/resolve-comments/references/review-format.md:46`)
- **[Claude]** No explicit post-edit validation loop for syntax/status/history correctness. → Add final validation and repeat-until-clean loop. (`skills/resolve-comments/SKILL.md:51`)

### review-resolve

#### P1 — Blocking
- **[Claude]** Mandatory format source points to `docs/how-to/communicate-during-plan-reviews.md`, but that path is missing, causing a broken required reference. → Change references to bundled `references/communicate-during-plan-reviews.md` or add the missing docs path. (`skills/review-resolve/SKILL.md:14`)

#### P2 — Important
- **[Claude]** Description uses imperative phrasing instead of third person. → Rewrite description in third person while preserving trigger phrases. (`skills/review-resolve/SKILL.md:3`)
- **[Claude]** No explicit verification loop after edits for format/status consistency. → Add final validation step and iterate until clean. (`skills/review-resolve/SKILL.md:65`)

### review-task

#### P2 — Important
- **[Claude]** Bundled reference `references/communicate-during-plan-reviews.md` is not linked from `SKILL.md`, reducing discoverability under progressive disclosure. → Link it from `SKILL.md` where comment-format guidance is discussed, or remove if unused. (`skills/review-task/SKILL.md:10`)

#### P3 — Minor
- **[Claude]** Malformed code-fence sequence in `communicate-during-plan-reviews.md` makes examples ambiguous. → Close `Resolving` fence before `Rejecting`, then open a separate fence. (`skills/review-task/references/communicate-during-plan-reviews.md:40`)

### task-feedback-analyser

No issues found.
