Build a calculator CLI application that can add two numbers.

Requirements:
- The user runs it from the command line, passing two numbers as arguments
- It prints their sum to stdout

Before writing any code, design a solid testing strategy for this project. Then implement the application and tests according to that strategy.
