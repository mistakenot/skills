You have a TanStack Start (React) expense tracker application in this workspace. Take a few minutes to study the codebase structure and understand what the application does.

The application tracks personal expenses with categories (food, transport, housing, entertainment, other), provides monthly summaries with averages, and supports filtering by category. It uses TanStack Start with server functions for the data layer and an in-memory store.

Your task:

1. Study the codebase and identify the critical testing surfaces
2. Write a testing strategy document (TESTING.md) that explains what to test and why
3. Set up the appropriate test framework(s)
4. Implement the tests
5. Make sure all tests can be run with a single command (e.g., a Makefile target or npm script)
6. Run the full test suite and get every test to pass before you finish — fix any bugs in the application code that your tests uncover

Focus on writing tests that would catch real bugs — not just happy-path smoke tests. Think about edge cases, boundary values, and the interaction between the different layers of the application.
